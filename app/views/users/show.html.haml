.row
  .span6.offset2.content
    .posts
      .bios.span4
        %ul.grav.span4.unstyled
          %li= gravatar_for(@user, size: "80")
          %li
            %h3= @user.name
        .bio.span4
          %p= @user.bio
        .contact.span4
          %p= icon("envelope", @user.email)
  = render 'shared/side_links'